function bodyload() {
    //access element by class attribute of element
    // getElementsByClassName method
    var count = document.getElementsByClassName("bstyle");
    alert("Number of total 'bstyle' classes on the webpage : " + count.length);
}